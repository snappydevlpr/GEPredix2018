#!/bin/bash
set -x
set +e
COUNT=""

appsFound=`cf a | grep $1 | wc -l`
svcsFound=`cf s | grep $1 | wc -l`

COUNT="$((appsFound + svcsFound))"

if (( COUNT > 0 )); then
 COUNT="NON-ZERO"
fi

echo $COUNT
