#!/bin/sh
# Assumption that you are org-migiration repo root level
set -x
echo "releaseToExternal.sh"
echo $1
echo $2
echo $3
echo $4
echo $GITHUB_REPO
echo $GITHUB_BRANCH
chmod 755 library/scripts/releaseRepo.sh
library/scripts/releaseRepo.sh -u $1 -p $2 -v $3 -m $4 -g $GITHUB_REPO -b $GITHUB_BRANCH 
