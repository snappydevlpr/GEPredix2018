#!groovy

/**
* Create static functions in the "vars" directory that can be invoked from any Jenkinsfile.
*  These functions should not store any state, only return values.
*/

/**
* Say hello
* @param firstname of person to greet
*/
def hello(String firstName) {
    echo "Hello, ${firstName} !"
}